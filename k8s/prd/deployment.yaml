apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-food-users
  namespace: users
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-food-users
  template:
    metadata:
      labels:
        app: api-food-users
    spec:
      containers:
        - name: api-food-users
          image: postechf57/pos-tech-g57-app-users:0.0.5
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: api-config-users
            - secretRef:
                name: users-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
